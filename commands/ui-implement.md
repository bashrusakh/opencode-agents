---
description: "Implement an already-understood UI/web change or accepted redesign plan in the existing frontend architecture."
agent: ui-implementer
subtask: false
---

Implement the UI/web change for: $ARGUMENTS

Follow the active `AGENTS.md`, ui-implementer contract, and applicable UI policy.

The direction/scope must already be understood. Reuse the project's existing components, tokens, styles, state/data patterns, and affordance class. Keep the change focused and preserve behavior outside the authorized UI contract.

If the request still requires a materially different design/product decision, stop at that ambiguity instead of inventing the direction. New dependencies/design systems/fonts/icon sets or broader product/API behavior remain subject to the root gate.

Verify the changed UI behavior and closest preserved behavior with the narrowest meaningful project checks.
