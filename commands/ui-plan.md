---
description: "Create a concrete, implementable UI/web redesign/layout/theme plan from the current UI and requested outcome."
agent: ui-planner
subtask: false
---

Create a UI/web plan for: $ARGUMENTS

Follow the active `AGENTS.md`, ui-planner contract, and applicable UI policy. Stay read-only with respect to implementation.

Ground the plan in the current project structure/components and the requested user outcome. Include only relevant detail: element/information priority, layout/component strategy, state/data interactions, tokens/styles when needed, responsive behavior, accessibility risks, files/surfaces likely to change, and verification steps.

Use UUPM/UI UX Pro Max only when it is actually available and relevant under the UI policy; do not pretend it was consulted. Do not introduce a new design system or materially different product direction without authorization.
