---
description: "Propose grounded UI/web redesign directions without implementing code."
agent: ui-orchestrator
subtask: false
---

Propose UI/web options for: $ARGUMENTS

Follow the active `AGENTS.md`, ui-orchestrator contract, and applicable UI policy. This is options-only: do not implement repository changes.

Understand the existing UI/project patterns only as deeply as needed, using UI specialists semantically when useful. Produce only materially distinct, feasible directions that genuinely exist; do not manufacture a fixed number of cosmetic alternatives.

For each option state the intended user outcome, main layout/interaction changes, affected surfaces/components, trade-offs, implementation risk/scope, and what project evidence supports it. Recommend one when the evidence is strong enough; otherwise state the unresolved choice.

Stop at the decision boundary. Do not invoke implementation merely because options were produced.
