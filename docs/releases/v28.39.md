# v28.39 — README release-history cleanup

This release changes bundle documentation only. Agent, command, skill, OCR, review, routing, verification, and publication behavior is unchanged from v28.38.

## Changed

- README carries only the current release summary instead of repeating multiple prior `What changed` sections.
- README links `CHANGELOG.md` once as the release-history entry point.
- Documentation map contains the current release note and durable policy/reference documents, not a version-by-version release-note index.
- Historical release notes remain under `docs/releases/` for direct historical lookup.
