# v30.8 beta — provenance pointer and Markdown precision cleanup

v30.8 beta is a precision/cosmetic release on top of v30.7 beta. Runtime behavior is unchanged.

## Changes

- Clarifies the root Startup/current-state provenance sentence so `git-provenance` is explicitly the canonical detailed provenance policy and root §8 is its workflow binding/activation point.
- Changes the README compact-root validation target from ambiguous `~45 KB` wording to `<50 KiB`.
- Normalizes extracted `git-provenance` and `pr-readiness` subsection headings from H4 (`####`) to H2 (`##`).
- Updates the explicit `AGENTS.md` pack marker and current-version documentation to v30.8 beta.

## Unchanged

- 15 agent roles, role frontmatter/modes/permissions, and execution topology.
- 18 bundled skills and their activation conditions.
- Resource lifecycle / cleanup reconciliation and gated remote deletion.
- Entry-routing vs delegation-routing behavior.
- Visual-evidence transport semantics.
- Installers.
