# v30.16 beta

Removes the last installer-only migration knowledge associated with the retired context-compaction experiment.

## Changes

- Removes the retired-skill list and retired-skill directory cleanup path from both global and project-local installers.
- Removes the now-unused tree-cleanup helper that existed only for that migration path.
- Keeps cleanup for exact obsolete package-owned command/doc/snippet filenames unchanged.
- Does not inspect, back up, remove, or otherwise mutate previously installed skill directories that are absent from the current package.

## Unchanged

- Runtime policy and the 18 bundled skills from v30.15.
- All 15 agent role contracts, including the v30.14 native edit/patch mutation semantics.
- Routing, goal/path ownership, permissions, provenance, PR readiness, verification, and publication gates.