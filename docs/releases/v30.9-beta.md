# v30.9 beta

Cosmetic/documentation-only release.

## Changed

- Replaces the version-pinned project-local README install example with a version-agnostic package path:

  ```bash
  /path/to/opencode-agent-pack/install/install-project.sh
  ```

- Updates package/version references to v30.9 beta.

## Unchanged

Runtime policy, routing, permissions, role contracts, skill inventory, resource cleanup semantics, gated deletion semantics, and installer behavior are unchanged from v30.8 beta.
