# v30.7 beta — version marker and documentation cleanup

v30.7 beta is a cosmetic/identification release on top of v30.6 beta. Runtime behavior is unchanged.

## Changes

- Adds `**Pack version: v30.7 beta**` directly under the root `AGENTS.md` title so the installed rules file identifies its own package version.
- Renumbers `§8.3 Visual evidence publication` to `§8.1 Visual evidence publication`; no policy text changes.
- Moves the changelog summary to the top of `CHANGELOG.md` and removes the stray extra blank line around the v30.4/v30.2 boundary.
- Updates README current-version references to v30.7 beta.

## Unchanged

- 15 agent roles and all role frontmatter/modes/permissions.
- 18 bundled skills and all skill activation conditions.
- Entry-routing vs delegation-routing topology.
- Resource lifecycle / cleanup reconciliation and gated remote deletion.
- Visual-evidence semantics and publication fallback policy.
- Installers.
